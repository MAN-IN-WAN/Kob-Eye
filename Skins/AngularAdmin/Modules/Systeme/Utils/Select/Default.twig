<div class="form-group row">
    <div class="col-md-12">
        <div class="input-group">
            <input type="text" class="form-control" name="{{ identifier }}Store.query" ng-model="{{ identifier }}Store.filter" placeholder="Recherche..." >
            <span class="input-group-btn">
                <a href="javascript: void(0);" class="btn btn-success" ng-click="{{ identifier }}Store.refresh()">
                    Rafraîchir
                </a>
            </span>
        </div>
    </div>
    <div class="col-md-6">
        <dir-pagination-controls boundary-links="true" pagination-id="{{ identifier }}-pagination" template-url="/Skins/AngularAdmin/assets/vendors/dirPagination/dirPagination.tpl.html"></dir-pagination-controls>
    </div>
</div>
<table class="table table-hover nowrap margin-bottom-0" id="{{ identifier }}" width="100%">
    <thead>
        <tr>
            <th>ID</th>
            <th>Label</th>
            <th >Actions</th>
        </tr>
    </thead>
    <tbody>
    <tr dir-paginate="item in {{ identifier }}Store.data" current-page="{{ identifier }}Store.currentPage" items-per-page="{{ identifier }}Store.itemsPerPage" pagination-id="{{ identifier }}-pagination">
        <td><span class="label label-warning">{[{ item.id }]}</span></td>
        <td scope="row">
            <span ng-bind-html="item.label"></span>
        </td>
        <td >
            <switch id="enabled" name="enabled" checklist-model="modalObj.{{ Field }}" checklist-value="item.id" class="green"></switch>
        </td>
    </tr>
    </tbody>
</table>
<dir-pagination-controls boundary-links="true" pagination-id="{{ identifier }}-pagination" template-url="/Skins/AngularAdmin/assets/vendors/dirPagination/dirPagination.tpl.html"></dir-pagination-controls>
