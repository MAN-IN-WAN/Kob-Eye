{
"total": "{{ total }}",
"data": [
{% for issue in issues %}
    {
    "id":"{{ graph.id }}",
    "label":"{{ graph.label }}",
    "delay":"{{ graph.delay }}"
    }{% if not loop.last %},{% endif %}
{% endfor %}
]
}