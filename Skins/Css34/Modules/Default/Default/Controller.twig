app.controller('{{ controller }}Ctrl', function($location, $scope, $rootScope,$http,{{ identifier }}Store) {
    //test du store
    $scope.currentPage = 1;
    $scope.{{ identifier }} = {{ identifier }}Store;
    {{ identifier }}Store.getData($scope.currentPage);
});