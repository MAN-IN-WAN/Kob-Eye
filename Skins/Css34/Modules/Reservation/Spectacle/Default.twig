{% if interface == 'List' %}
    {{ include(template_from_string(module('Reservation/Spectacle/ListBundle'))) }}
{% else %}
    {{ include(template_from_string(module('Reservation/Spectacle/FicheBundle'))) }}
{% endif %}