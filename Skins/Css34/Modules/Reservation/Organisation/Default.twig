{% if interface == 'List' %}
    {{ include(template_from_string(module('Reservation/Organisation/ListBundle'))) }}
{% else %}
    {{ include(template_from_string(module('Reservation/Organisation/FicheBundle'))) }}
{% endif %}